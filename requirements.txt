# Core dependencies (installed with `pip install -e .`)
numpy>=1.24.0
Pillow>=10.0.0
tqdm>=4.65.0
pandas

# Optional extras — install with `pip install -e ".[<group>]"`
#   segmentation: torch, torchvision, opencv-python, scikit-image,
#                 iopath, huggingface-hub, einops, pycocotools, ftfy
#   cleaning:     imagehash
#   video:        opencv-python
#   augment:      albumentations
#   classify:     autogluon.multimodal, timm, umap-learn, matplotlib,
#                 grad-cam, onnxruntime, scikit-learn, setuptools<70
#   dev:          pytest, pytest-cov
